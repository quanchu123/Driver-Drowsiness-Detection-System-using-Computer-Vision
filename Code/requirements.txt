# Core realtime (optimized, no training required)
numpy>=1.24
opencv-python-headless>=4.8
mediapipe>=0.10.9
pyyaml>=6.0

# Optional — only if you run train/audit scripts later
# pandas>=2.0
# scikit-learn>=1.3
# joblib>=1.3
# xgboost>=2.0
